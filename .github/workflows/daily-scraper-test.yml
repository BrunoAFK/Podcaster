name: 🗓️ Daily Scraper Feed Validation

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Create fake .env
        run: |
          cat <<EOF > .env
          # ---------- osnovno ---------- #
          DOMAIN=doman.tld
          HOST_PORT=3456

          # ---------- feedovi ---------- #
          FEEDS=jutarnja-kronika:jk.xml,vijesti:v.xml

          # ---------- telegram notifikacije ---------- #
          TELEGRAM_BOT_TOKEN=token:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAA
          TELEGRAM_CHAT_ID=1234567890
          TELEGRAM_NOTIFICATIONS_ENABLED=false
          TELEGRAM_NOTIFICATION_TYPES=error

          MAX_EPISODES=30
          LOG_RETENTION_DAYS=7
          EOF

      - name: Set up Docker Compose
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose

      - name: Pull images / dependencies
        run: docker compose pull

      - name: Run “single” scrape for all feeds
        run: |
          for svc in $(docker compose config --services | grep '_feed$'); do
            docker compose run --rm "$svc" /app/shared/run.sh --quiet
          done

      - name: Run “catch-all” scrape for all feeds
        run: |
          for svc in $(docker compose config --services | grep '_feed$'); do
            docker compose run --rm "$svc" /app/shared/run.sh --fetch-all --quiet
          done

      - name: "Install tree for debugging"
        run: |
          sudo apt-get update
          sudo apt-get install -y tree

      - name: "Debug: show workspace layout"
        run: |
          echo "===== project root tree ====="
          tree -a -L 2 .
          echo
          echo "===== listing project root ====="
          ls -la .
          echo
          echo "===== listing .github directory ====="
          if [ -d ".github" ]; then
            ls -la .github
          else
            echo "⚠️  .github directory not found"
          fi
          echo
          echo "===== listing tests directory ====="
          if [ -d "tests" ]; then
            ls -la tests
          else
            echo "⚠️  tests directory not found"
          fi
          echo
          echo "===== listing feeds directory ====="
          if [ -d "feeds" ]; then
            ls -la feeds
          else
            echo "⚠️  feeds directory not found"
          fi
          echo
          echo "===== listing scripts_jk & scripts_v directories ====="
          for d in scripts_jk scripts_v; do
            if [ -d "$d" ]; then
              echo "Contents of $d:"
              ls -la "$d"
            else
              echo "⚠️  $d directory not found"
            fi
          done



      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install test requirements
        run: |
          pip install --upgrade pip
          pip install xmlschema

      - name: Validate generated RSS XML (if test exists)
        run: |
          if [[ -f tests/test_feeds.py ]]; then
            echo "➤ Found tests/test_feeds.py, running validation..."
            python tests/test_feeds.py
          else
            echo "⚠️  tests/test_feeds.py not found, skipping validation."
          fi
